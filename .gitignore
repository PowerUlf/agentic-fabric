# Secrets — never commit
.env
.env.*
!.env.example

# Python
.venv/
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.pytest_cache/
.ruff_cache/

# Working plan — kept locally, deliberately not published.
docs/plan.md

# Journal / local state
*.jsonl
.afabric/

# Node (TypeScript UI, later)
node_modules/

# Microsoft Fabric VS Code extension: workspace sync artifacts.
# Each synced workspace lands in a directory named after its UUID.
.work-folder-info
/????????-????-????-????-????????????/

# Editors / OS
.DS_Store
.vscode/
.idea/
